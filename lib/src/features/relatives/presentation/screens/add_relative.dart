import 'package:flutter/material.dart';

class AddNewRelative extends StatelessWidget {
  const AddNewRelative({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
