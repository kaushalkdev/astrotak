import 'package:flutter/material.dart';

class EditRelative extends StatelessWidget {
  const EditRelative({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
